#!/usr/bin/env perl

my $path	= shift;

my $id = `git log --pretty=format:"%H %aI %an" -1 -- $path`;

while (my $line = <STDIN>) {
	$line =~ s/# \$COBRA[^\$]*\$/# \$COBRA: $path $id \$/;
	print $line;
}
